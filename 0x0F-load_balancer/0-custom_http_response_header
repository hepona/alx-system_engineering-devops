#!/usr/bin/env bash
#double the number of webservers
sudo apt update
sudo apt install -y nginx
name=$(hostname | cut -d '-' -f2-)
# echo "X-Served-By: '$name' " > sudo tee var/www/html/index.html
echo "X-Served-By: $name" | sudo tee /var/www/html/index.html
sudo service nginx start